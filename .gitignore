/target
**/__pycache__/
*.pyc

# data is the user's, not the repo's: pulled corpora (see skills/*/SKILL.md),
# derived caches, and the twin's own journals stay local…
/data/*
/data/projects/*

# …except the authored Volve drilling samples (test fixtures + offline demo)
# and the seeded demo project that mounts them
!/data/volve/
!/data/projects/
!/data/projects/volve/

# the pulled REAL corpus is data, not repo (obtain-volve github/pull)
/data/volve/real/

# app-shell build artifacts + the local server log
/tauri/gen/schemas/
serve.log

# the showcase ships with the repo: generated demo data + its seeded project
!/data/showcase/
!/data/projects/showcase/
